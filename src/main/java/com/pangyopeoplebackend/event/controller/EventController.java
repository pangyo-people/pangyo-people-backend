package com.pangyopeoplebackend.event.controller;

/* @RestController
@RequiredArgsConstructor
public class EventController {

    private final EventService eventService;

    @PutMapping(path = "/v1/api/admin/event/{id}")
    public Event permitEvent(@PathVariable String id){
        return eventService.permitEvent(id);
    }

 @PostMapping("/v1/api/events/write")
    public Event saveOrg(@RequestBody Event event) {
        return eventService.saveEvent(event);
    }
}
*/
